# Upstash Redis Configuration
# Get these from https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-database-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-database-token

# Optional: Vercel Blob Storage (for image uploads)
# BLOB_READ_WRITE_TOKEN=your-blob-token

# Admin CMS authentication
# Default admin (will be copied to Upstash on first run if configured)
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
# EITHER set ADMIN_PASSWORD (plain, for local dev) OR ADMIN_PASSWORD_HASH (bcrypt hash) in production
# ADMIN_PASSWORD=super-secret
# ADMIN_PASSWORD_HASH=$2a$12$...
# Optional: enable TOTP MFA (base32 secret)
ADMIN_TOTP_SECRET=JBSWY3DPEHPK3PXP
# Secret used to sign session JWTs
AUTH_SECRET=replace-with-strong-random

# Security toggles (do NOT enable in production)
# ALLOW_DEBUG_ROUTES=1
# ALLOW_DEMO_OTP=1

# Session TTL (e.g., 1d, 12h). Defaults to 1d if unset.
# SESSION_TTL=1d
